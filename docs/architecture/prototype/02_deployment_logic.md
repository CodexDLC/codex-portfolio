# 🚀 Логика структуры и деплоя

[📂 Prototype Index](./README.md) > 📄 Deployment Logic

---

## 🏠 Иерархия папок (Project Tree)
```plaintext
/ (root)
├── index.html              # Входная точка для GitHub Pages
├── src/
│   ├── prototype/          # Изолированная песочница для набросков
│   │   ├── css/
│   │   └── js/
│   └── website/            # Будущая чистовая реализация (Django/Static)
├── assets/                 # Общие ресурсы (лого, диаграммы)
└── docs/                   # Техническая документация
```

---

## ⚙️ Механика прототипа
*   **Root Bridge:** Файл `index.html` в корне проекта на этапе прототипирования будет содержать структуру главной страницы, обращаясь к ресурсам из `/src/prototype/`.
*   **Isolation:** Все экспериментальные скрипты и стили хранятся строго в `src/prototype/`, не затрагивая папку `website`.
*   **Future Move:** Когда прототип будет утвержден, лучшие решения будут перенесены (рефакторинг) в `src/website/` с учетом требований Django.