# CSS — краткие пояснения (RU)

[⬅️ Back](../README.md) | [🏠 Root](/docs/README.md)

Здесь кратко описаны ключевые моменты CSS из прототипа и ссылки на подробные
англоязычные
спецификации.

Основные идеи:

- Используется набор дизайн-токенов (`:root` переменные) — цвета, размеры,
  брейкпоинты.

- Фиксированная обёртка для `header`/`footer` централизуется через `left:50%` +
  `transform` и `--max-
  width`.

- Компоненты: `header`, `slides-container`, `bento-grid`, `bento-card`.

См. подробности (EN):

- Переменные: [EN: variables.md](../../en/templates/CSS/variables.md)

- Компоненты: [EN: components.md](../../en/templates/CSS/components.md)

<!-- DJANGO: при миграции используйте эти ссылки как исходный контракт -->
