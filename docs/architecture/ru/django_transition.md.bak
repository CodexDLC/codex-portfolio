# 📄 Transition to Django (Future Spec)

[📂 Architecture Context](./README.md) > 📄 Django Transition

##

## 🎯 Цель

Зафиксировать архитектурные изменения при переходе от статического прототипа к динамическому
приложению на Django.

##

## 🏗️ Dynamic Layers (Динамические слои)

В отличие от прототипа, где все вкладки загружены сразу и скрыты через CSS (`display: none`), в
Django мы переходим на **Server-Side Rendering (SSR)** или **HTMX**.

*   **Logic:** Каждая вкладка (Clients, Products, Open Source) инициирует запрос к БД.

*   **Optimization:** Контент подгружается только по требованию, снижая начальный вес страницы.

##

## 🏝️ Vertical Islands (Вертикальные острова)

При запросе категории Django генерирует страницу с вертикально расположенными «островами» проектов.

*   **Structure:** Вместо горизонтальных слайдеров или табов, проекты выстраиваются в бесконечную ленту
    (Infinite Scroll или Pagination).

*   **Bento Grid:** Внутри каждого "острова" сохраняется логика Bento-сетки для адаптивности.

##

## 🔄 Index Logic (Единообразие UX)

Полное копирование визуальной логики главной страницы (Index) для внутренних разделов.

*   **Consistency:** Пользователь не должен чувствовать разницу между "Главной" и "Портфолио".

*   **Components:** Переиспользование шаблонов карточек (`card.html`) и сетки (`bento_grid.html`).
