# 🎨 CSS Design Tokens & Migration Notes

[⬅️ Back](../README.md) | [🏠 Root](/docs/README.md)

Этот файл описывает не просто список переменных, а контракт дизайна — какие
токены важны при
миграции в Django и как ими управлять.

## Цель

- Сформировать минимальный набор дизайн-токенов, которые должны переноситься в
  `static/css/` как
  `:root` переменные.

- Дать указания по связке токенов с Django settings или внешним источником (в
  будущем).

##

## Обязательные токены (контракт)

Сохраните эти переменные в `:root` — они используются в компонентах и в
документации как исходная
спецификация.

```
--color-void
--color-monolith
--color-gold
--color-blue
--color-ivory
--color-ghost

--max-width
--header-total-h
--footer-height

--font-primary
--font-mono

--transition-fast
--transition-normal
--transition-slow

```

**Примечание:** при изменении значений в `base.css` обновляйте `variables.md`
(этот файл) — он
служит single source of truth для дизайна.

##

## Как документировать изменения токенов

1. При добавлении нового цвета/размера — обновляйте `variables.md` с коротким
rationale (зачем нужен
токен).

2. Для каждой новой переменной укажите область влияния (например: `--color-
accent` — используется в
`.bento-card-title`).

##

## Связь с Django

- Можно хранить базовые токены в `settings.py` (опционально) и рендерить в `:root`
  через шаблон:

```django
<style>
:root {
	--color-gold: {{ DESIGN_TOKENS.color_gold }};
}
</style>

```

- Для быстрого прототипирования пока копируем `:root` как статический файл.
  Перевод в runtime-
  управляемые токены — опционален.

##

## Breakpoints и адаптивность — рекомендации

- Использовать централизованный набор брейкпоинтов: mobile (<=767px), tablet (768-1279px), desktop (1280-1440px), large (>=1441px).

- Документировать поведение компонентов при переходе между брейкпоинтами
  (например: `bento-grid`
  меняет число колонок).

##

## i18n & тексты, связанные с токенами

- Если токен влияет на отображение текста (например, `--font-size-base`),
  отмечайте в `I18N_KEYS.md`,
  чтобы переводы учитывали длину строк в разных языках.

##

## Проверка

1. После изменения токенов — проверить все ключевые страницы (hero, bento,
portfolio) на
desktop/tablet/mobile.

2. Добавить визуальные тесты (скриншоты) при желании.

