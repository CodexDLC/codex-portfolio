# 🏗️ Компоненты и принципы адаптивной верстки

[⬅️ Back](../README.md) | [🏠 Root](/docs/README.md)

Этот документ не копирует CSS целиком — он описывает принципы, стадии и
контрактные точки, которые
важно сохранить при переносе прототипа в Django-шаблоны и статические файлы.

## Цели документа

- Объяснить архитектуру адаптивной верстки прототипа

- Выделить блоки и точки расширения для Django-шаблонов (`{% include %}`, `{%
  block %}`)

- Дать рекомендации по разделению ответственности между HTML (шаблоны), CSS
  (статические файлы) и JS
  (поведение)

##

## Структура адаптивной верстки (высокоуровнево)

1. Layout container: фиксированные элементы (`header`, `footer`) центрируются
через `left:50% +
transform: translateX(-50%)` и ограничиваются `--max-width`.

2. Основная область — `.slides-container` занимает оставшуюся высоту между
`header` и `footer`.

3. Каждый экран реализован как `.slide` (hero, bento и т.д.) — переключается
через классы `active`,
`hidden-up`, `hidden-down`.

4. Карточки в Bento Grid — адаптивная сетка (`grid-template-columns:
repeat(auto-fit, minmax(...))`)
для плавного изменения числа колонок.

##

## Стадии переноса CSS/HTML → Django

1. Скопировать CSS/JS как статические файлы в `static/css/` и `static/js/` (не
редактируя). Это
позволяет быстро запустить шаблоны.

2. Создать `base.html` с глобальными блоками: `head`, `header`, `content`,
`footer`, `extra_css`,
`extra_js`.

3. Разбить повторяемые фрагменты на частичные шаблоны (`includes`):
`includes/header.html`,
`includes/footer.html`, `includes/bento_card.html`.

4. В child-шаблонах переопределить блок `content` и подключить page-specific
CSS/JS через
`extra_css`/`extra_js`.

5. Заменить относительные пути к ассетам на `{% static %}` и все видимые строки
— на `{% trans %}`
или i18n-ключи.

##

## Контрактные точки (что документировать/сохранить)

- ID / классы слайдов: `#slide-hero`, `#slide-bento` — не менять, JS на них
  опирается.

- Классы состояния: `active`, `hidden-up`, `hidden-down` — оставить для CSS/JS.

- Навигация: `.header-nav`, `.nav-burger` — вынести в `includes/header.html` и
  заполнять ссылками
  через Django `url`.

- Карточки: создать `includes/bento_card.html` с контекстом `{% include
  'includes/bento_card.html'
  with title=card.title description=card.desc %}`.

##

## Примеры превращения HTML-фрагментов в шаблоны

- Static header (prototype):

```html
<header class="layout-header"> ... nav links ... </header>

```

→ Django: `includes/header.html`

```django
<header class="layout-header">
  <div class="header-logo">{{ site_logo_html|safe }}</div>
  <nav class="header-nav">
    <a href="{% url 'home' %}" class="nav-link">{% trans "nav_home" %}</a>
    <!-- other links -->
  </nav>
  <button class="nav-burger">...</button>
</header>

```

##

## Рекомендации по именованию и слоям

- Классы — семантические, не завязанные на JS-логике (`.hero-title` вместо `.h1--
  01-js`).

- JS-слой опирается на `id` и на `data-` атрибуты (`data-tab`, `data-dna`) — эти
  атрибуты должны
  сохраниться.

- Компоненты с данными — рендерить через контекст Django (модель/сериализатор) или
  через включение
  `include`.

##

## i18n и тексты

- Все видимые тексты должны иметь i18n-кейсы (`<!-- i18n: key -->` в документации).

- В шаблонах использовать `{% trans "key" %}` или передавать строки из view через
  `gettext_lazy`.

##

## Проверка после переноса

1. Запустить сервер Django и открыть страницы: проверьте, что стили подключены и
`static` пути
корректны.

2. Проверить поведение слайдера (wheel, mobile) — если классы не совпадают, JS
не сработает.

3. Тест i18n: сменить LANGUAGE_CODE или использовать `?lang=ru` и убедиться, что
теги `{% trans %}`
работают.

##

## TODO / Возможные улучшения

- Перевести повторяющиеся CSS значения в дизайн-токены и держать их в
  `variables.md`.

- Рассмотреть разбивку больших CSS-файлов на логические модули при миграции
  (header.css, hero.css,
  bento.css).

- Автоматизировать проверку (CI) на совпадение id/классов между HTML и JS.

