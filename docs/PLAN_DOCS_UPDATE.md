# 📋 План актуализации документации Backend (2026)

Этот документ фиксирует расхождения между кодом (`src/backend`) и документацией (`docs/`), выявленные в ходе аудита.

## 1. Анализ расхождений (Gap Analysis)

### 🔴 Приложение `portfolio`
**Файл:** `docs/.../apps/portfolio/data_schema.md`
**Статус:** Требует полной переработки.

| Сущность | Документация (Устарело) | Код (Актуально) | Что делать |
| :--- | :--- | :--- | :--- |
| **Category** | Поле `title` | Поле `name` | Переименовать |
| | `theme_color` | `theme_class` | Обновить |
| | `animation_preset` | *Удалено* | Удалить из доков |
| | *Отсутствует* | `tab_filename`, `tab_icon_text`, `tab_icon_class` | Добавить описание табов |
| | *Отсутствует* | `section_number`, `section_title` | Добавить описание секций |
| **Project** | `layout_type` (Enum) | *Вычисляется динамически* | Описать сервис Layout |
| | `extra_data` (JSON) | *Удалено* | Удалить |
| | `cover_image` | *Отсутствует* | Удалить |
| | *Отсутствует* | `style_visual` (FK -> StyleAttribute) | Добавить связь |
| | *Отсутствует* | `tags` (M2M -> TechTag) | Добавить связь |
| | *Отсутствует* | `views_count`, `likes_count` | Добавить метрики |
| | *Отсутствует* | `status`, `project_type` | Добавить Enums |

### 🔴 Приложение `system`
**Файл:** *Отсутствует*
**Статус:** Критический пробел.

В коде существуют модели `TechTag` и `StyleAttribute`, которые являются фундаментальными для классификации и дизайна, но они нигде не описаны в документации архитектуры.

## 2. План действий (Action Plan)

### Этап 1: Создание документации для System
1.  Создать директории:
    *   `docs/en_EN/architecture/backend/apps/system/`
    *   `docs/ru_RU/architecture/backend/apps/system/`
2.  Создать файл `data_schema.md` в этих папках:
    *   Описать модель **TechTag** (Категории: backend, frontend...; Иконки).
    *   Описать модель **StyleAttribute** (Типы: layout, visual...; Target Areas).

### Этап 2: Актуализация Portfolio
1.  Отредактировать `docs/.../apps/portfolio/data_schema.md`:
    *   Привести поля `Category` и `Project` в соответствие с `models.py`.
    *   Заменить раздел про "Single Source / JSON" на описание строгой типизации через Django Models.
    *   Добавить ссылки на документацию `system`.

### Этап 3: Обновление навигации
1.  Обновить `docs/en_EN/architecture/backend/README.md` (и RU):
    *   Добавить `System` в таблицу модулей.
