# 🧠 Паттерн Сервисный Слой (Service Layer)

[⬅️ Назад](../README.md) | [🏠 Документация](../../../../README.md)

> **ЦЕЛЬ:** Оставить `views.py` тонкими, а логику — тестируемой.
> **ПРАВИЛО:** Представления (Views) должны обрабатывать только HTTP (запрос/ответ). Вся логика находится здесь.

## 1. Разделение

Мы разделяем бизнес-логику на два различных типа функций: **Селекторы (Selectors)** и **Сервисы (Services)**.

| Тип | Путь | Назначение | Побочные эффекты? |
| :--- | :--- | :--- | :--- |
| **Селекторы** | `apps/*/selectors.py` | Получение данных (Чтение). | ❌ НЕТ (Только чтение) |
| **Сервисы** | `apps/*/services.py` | Изменение данных (Запись). | ✅ ДА (Создание/Обновление) |

## 2. Селекторы (Чтение)

Функции, которые возвращают QuerySets, списки или DTO. Они **никогда** не изменяют базу данных.

*Пример:* Функция `get_featured_projects()` возвращает список проектов, которые нужно показать на главной, уже отфильтрованных и отсортированных.

## 3. Сервисы (Запись)

Функции, которые обрабатывают сценарии использования (use-cases). Они принимают "сырые" аргументы, валидируют их и выполняют действие.

*Пример:* Функция `create_contact_message()` принимает имя и email, создает запись в БД и запускает отправку уведомления на почту.

## 4. Использование в Views

View просто вызывает нужный селектор или сервис. В самом View нет логики фильтрации или создания объектов.

---
*Примеры кода на Python см. в [английской версии](../../../../en/backend/patterns/service_layer.md).*
