# 🏠 Main App (RU)

[⬅️ Назад](../README.md) | [🏠 Документация](../../../../../README.md)

> **ОБЛАСТЬ:** `src/backend/apps/main/`
> **НАЗНАЧЕНИЕ:** Статические страницы, композиция главной страницы, формы контактов.

## 1. Обязанности

Приложение `main` выступает в роли "лица" веб-сайта. Оно агрегирует данные из других приложений (например, `portfolio`) для отображения на главной странице.

*   **Главная страница:** Секция Hero + Bento Grid (данные из приложения Portfolio).
*   **Опыт (Experience):** Визуализация временной шкалы.
*   **Контакты (Contact):** Обработка форм и логика отправки email.

## 2. Представления (Views) и Логика

### A. Главное представление (`IndexView`)

*   **Маршрут:** `/`
*   **Шаблон:** `main/templates/home.html`
*   **Контекст:**
    *   `featured_projects`: Получает `Project.objects.filter(is_featured=True)` из приложения Portfolio.
    *   *Примечание:* Это создает зависимость от `apps.portfolio`.

### B. Представление контактов (`ContactView`)

*   **Маршрут:** `/contact/`
*   **Логика:**
    1.  Валидирует `ContactForm`.
    2.  Сохраняет сообщение в БД (модель `ContactMessage`).
    3.  (В будущем) Асинхронно отправляет email через Celery.
    4.  Возвращает частичный HTMX-шаблон для состояния "Успешно".

## 3. UI Компоненты

Это приложение владеет глобальными компонентами-оболочками, расположенными в `templates/includes/`:

*   `_header.html`: Основная навигация.
*   `_footer.html`: Глобальный футер.
*   `_bento_section.html`: Обертка для макета сетки Bento.

---
*Полная техническая документация и имена моделей находятся в [английской версии](../../../../../en_EN/architecture/backend/apps/main/README.md).*
