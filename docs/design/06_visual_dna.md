# 📄 Visual DNA & Motion (Визуальный ДНК и Анимации)

[📂 Design Index](./README.md) > 📄 Visual DNA

---

## 🎯 Описание
Этот документ описывает "Живой визуальный язык" (Visual DNA). Мы не используем одинаковые анимации для всего. Каждый тип проекта имеет свой уникальный характер движения и визуальной обратной связи.

---

## 1. Философия "Живых Карточек"
Пользователь должен различать типы проектов не только по тегам, но и на подсознательном уровне — через динамику фона, скорость анимации и тип свечения.

* **Принцип:** Анимация — это часть навигации.
* **Реализация:**
    * *Stage 1 (CSS Prototype):* Градиенты, статические паттерны, `box-shadow`.
    * *Stage 2 (Canvas/WebGL):* Интерактивные частицы, шейдеры, параллакс.

---

## 2. Типология ДНК (The 3 DNA Types)

### 🏆 TYPE A: BUSINESS (Gold DNA)
Используется для флагманских продуктов, ERP-систем и Enterprise-решений.

* **Метафора:** Структура, Монолит, Золото, Стабильность.
* **Ключевые слова:** `Solid`, `Structure`, `Heavy`, `Premium`.
* **Цветовой код:** `Python Gold (#FFD43B)` + Deep Black.
* **Визуальные паттерны:**
    * Геометрические сетки (Grid).
    * Четкие линии, складывающиеся в фигуры (кубы, полигоны).
    * Медленное, "тяжелое" движение (как открытие сейфа).
* **CSS Эффект (MVP):** Темный фон + Золотое статичное свечение снизу (`box-shadow`) + Еле заметная сетка на фоне.

### ⚡ TYPE B: ECOSYSTEM (Blue DNA)
Используется для ботов, API, микросервисов и сетевых инструментов.

* **Метафора:** Поток, Сеть, Электричество, Связи.
* **Ключевые слова:** `Flow`, `Connection`, `Fast`, `Liquid`.
* **Цветовой код:** `Python Blue (#306998)` + Cyan accents.
* **Визуальные паттерны:**
    * Созвездия (Constellations) — точки, соединенные линиями.
    * Потоки данных (Data Streams).
    * Плавное, "текучее" движение.
* **CSS Эффект (MVP):** Синее "неоновое" свечение + Паттерн "микросхема" или точки.

### 🧪 TYPE C: R&D LABS (Ghost/Green DNA)
Используется для экспериментов, скриптов, парсеров и AI.

* **Метафора:** Терминал, Код, Глитч, Нестабильность.
* **Ключевые слова:** `Raw`, `Code`, `Glitch`, `Terminal`.
* **Цветовой код:** `Ghost Gray` / `Terminal Green`.
* **Визуальные паттерны:**
    * Моноширинный текст, бегущие строки.
    * Эффект помех (Glitch), сдвиг RGB-каналов.
    * Мерцание курсора.
* **CSS Эффект (MVP):** Пунктирная рамка (`dashed`) + Эффект "Scanline" (полоски монитора) + Тусклое зеленоватое свечение.

---

## 3. Правила переходов (Transitions)

### Hover-эффекты
При наведении курсора на карточку:
1.  **Business:** Увеличивается масштаб (Scale), рамка становится ярко-золотой. Ощущение веса.
2.  **Ecosystem:** Ускоряется движение частиц/фона. Ощущение энергии.
3.  **R&D:** Происходит короткий "сбой" (Glitch) или инверсия цветов. Ощущение цифровой природы.

### Page Transition (Hero Animation)
При переходе внутрь проекта:
* Фон карточки расширяется на весь экран, сохраняя свой "ДНК".
* *Пример:* Если нажали на Business, хедер новой страницы будет иметь золотую сетку.