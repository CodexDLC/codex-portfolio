# 📄 Page Flow & Transitions (Структура страниц и логика переходов)

[⬅️ Back](./README.md) | [🏠 Docs Root](../README.md)

## 1. Путь пользователя: Главная → Проект

Основная задача — сделать переход из сетки Bento в проект бесшовным.

* **Триггер:** Клик по карточке проекта в Bento Grid.
* **Анимация (Hero Transition):**
  * Выбранная карточка расширяется на весь экран, становясь «фоном» для верхней секции проекта.
  * Остальные карточки Bento плавно исчезают (Opacity 0).
* **Результат:** Пользователь мгновенно оказывается в верхней части «Тройного сплита», не теряя визуального контекста.

## 2. Логика «Тройного Сплита» (Project Detail)

Верхняя часть страницы проекта — это интерактивный хаб.

### A. Взаимодействие со Сплит-блоками (Header)

* **Состояние покоя:** Левая (Prototype) и Правая (Production) панели равнозначны по ширине (50/50).
* **Hover-эффект:** При наведении на одну из панелей она слегка расширяется (например, до 60%), а соседняя сужается. Это акцентирует внимание на выбранном пути.
* **Клик:** При клике на `[ Open Link ]` открывается соответствующий URL (сабдомен `codexdlc.dev` или основной сайт) в новой вкладке.

### B. Переход к презентации (Scroll Down)

* При начале скролла вниз верхний Сплит-блок плавно уезжает вверх или фиксируется в виде узкой полоски (Mini-header), уступая место основной презентации.

## 3. Презентационный поток (Slide Narrative)

Нижняя часть страницы работает по принципу «вертикальной презентации».

* **Sticky-скролл:** При скролле каждый следующий слайд (блок контента) может «наплывать» на предыдущий или плавно проявляться.
* **Интерактив в слайдах:**
  * Схемы Mermaid.js отрисовываются при попадании слайда во вьюпорт.
  * Если слайд содержит код, он подсвечивается (Syntax Highlighting) с эффектом «печатания».

## 4. Логика «Smart Accordion» (Mobile)

Реализация идеи для мобильных устройств, чтобы избежать «бесконечного слайдера».

* **Начальное состояние:** 3 узкие плашки (Business / Pet / Ideas).
* **Действие:** Нажатие на заголовок плашки.
* **Поведение:**
  1. Активная плашка раскрывается на высоту экрана (минус высота заголовков остальных плашек).
  2. Внутри плашки появляется вертикальный список проектов.
  3. При нажатии на другую плашку — текущая мгновенно сворачивается, а новая открывается.
* **Зачем:** Это сохраняет структуру и не дает пользователю потеряться в прокрутке огромного списка.

## 5. Брендинг-анимация (Header DLC Decoder)

* **При загрузке страницы:** В хедере отображается `CodexDLC`.
* **Через 1.5 секунды:** Буквы `DLC` плавно заменяются на `[ Developer’s Life Cycle ]` (эффект печатной машинки).
* **При скролле:** Текст снова сокращается до `CodexDLC`, чтобы не занимать место.
